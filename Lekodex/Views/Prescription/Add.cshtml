@model PrescriptionViewModel

@{
    ViewData["Title"] = "Prescription Page";
}

<form asp-controller="Prescription" asp-action="Add">
    <p>Name:</p>
    <input type="text" name="Name" />
    <button type="submit" class="btn btn-success">Add</button>
</form>
